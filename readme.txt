Not all files here are standalone applications; some are ROOT macros. The files which are standalone applications can be built with CMake:
```shell
$ mkdir build && cd build
$ cmake ..
$ make -j4 # number of cores, e.g. if you have 8 cores put -j8
```



































































bozo
